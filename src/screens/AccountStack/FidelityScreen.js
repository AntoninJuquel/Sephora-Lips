import React from 'react';
import { Text, View } from 'react-native';

import Styles from '../../utils/Styles';

function FidelityScreen() {
    return (
      <View style={Styles.container}>
        <Text>FidelityScreen works</Text>
      </View>
    );
  }
  
  export default FidelityScreen;